#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int T,N,i,max,j;
    scanf("%d",&T);
    for(i=0;i<T;i++){
        scanf("%d",&N);
        int a[N];
        for(j=0;j<N;j++){
            scanf("%d",&a[j]);
        }
        
        
    }
    return 0;
}
